<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class='bg-primary text-white text-center mb-4 p-4'>
      <%= link_to root_path, class: 'text-white text-decoration-none' do %>
        <h1 class='display-4'>Tech blog</h1>
      <% end %>

      <p class='lead'>
        Another tech blog, you'll find tech stuff here.
      </p>
    </div>

    <div class='container'>
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"/>
        </div>
      <% end %>

      <div class='row'>
        <div class='col-4 offset-8'>
          <%= form_with url: search_posts_path, method: :get, local: true, class: 'd-flex justify-content-end' do |form| %>
            <div class='form-group mx-3 mb-3'>
              <%= form.search_field :q, class: 'form-control', required: true, placeholder: 'Busca' %>
            </div>

            <%= form.submit 'Buscar', class: 'btn btn-primary mb-3', data: { disable_with: 'Buscando...' } %>
          <% end %>
        </div>
      </div>

      <%= yield %>
    </div>

    <footer class='bg-primary text-white text-center mt-5 p-4'>
      Copyright &copy; Someone <%= Date.current.year %>
    </footer>
  </body>
</html>
